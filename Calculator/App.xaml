<Application x:Class="Calculator.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Calculator"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="numbersColor" Color="#444444"  />
        <SolidColorBrush x:Key="operatorsColor" Color="Green"/>
        <SolidColorBrush x:Key="foreColor" Color="White"/> <!--#region apply all ui styles on full app-->

        <Style TargetType="Button">

            <Setter Property="Foreground" Value="White"></Setter>
            <Setter Property="FontSize" Value="25"/>
            <Setter Property="Margin" Value="5"/>
            
        </Style>

        <Style TargetType="Label">

            <Setter Property="FontSize" Value="70"/>
        </Style>
        <Style TargetType="Button" x:Key="NumberButtonsStyle">
            <Setter Property="Background" Value="{StaticResource numbersColor}"/>
            <Setter Property="Foreground" Value="White"></Setter>
            <Setter Property="FontSize" Value="25"/>
            <Setter Property="Margin" Value="5"/> 

        </Style>
        <Style TargetType="Button" x:Key="OperationsButtonsStyle">
            <Setter Property="Background" Value="{StaticResource operatorsColor}"/>
            <Setter Property="Foreground" Value="White"></Setter>
            <Setter Property="FontSize" Value="25"/> 
            
            <Setter Property="Margin" Value="5"/> <!--#region that means margin from all corners left,top,right,down-->


        </Style>
    </Application.Resources>
    
    
</Application>
